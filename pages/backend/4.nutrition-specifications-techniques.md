## 3. Spécifications techniques

### 3.1 Architecture globale

Le système sera implémenté comme une application monolithique déployée sur Kubernetes, qui interagira avec Keycloak pour l'authentification et une API externe pour les données alimentaires.

```
+------------------+       +------------------+       +------------------+
| Client Mobile    |       | Client Web       |       | Applications     |
| (Frontend)       |       | (Frontend)       |       | Tierces          |
+------------------+       +------------------+       +------------------+
         |                           |                         |
         +------------+-------------+--------------------------+
                      |
                      v
+------------------------------------------+
|           Kubernetes Gateway API         |
+------------------------------------------+
                      |
                      v
+------------------------------------------+
|        API Nutrition (ASP.NET Core)      |
|                 (Pods)                   |
+------------------------------------------+
         |                        |
         v                        v
+-------------------+    +-------------------+    +-------------------+
| PostgreSQL        |    | Keycloak          |    | API Food Externe  |
| (StatefulSet)     |    | (Deployment)      |    | (Open Food Facts) |
+-------------------+    +-------------------+    +-------------------+
```

### 3.2 Responsabilités du service

L'API Nutrition est conçue comme un service monolithique qui gère l'ensemble des fonctionnalités :

#### Gestion des utilisateurs

- Création et gestion des comptes utilisateurs (inscription, modification de profil)
- Interface avec Keycloak pour l'authentification et l'autorisation
- Gestion du profil utilisateur (données physiologiques, préférences, objectifs)
- Calcul du métabolisme de base (BMR) et des besoins caloriques
- Gestion des identifiants et tokens pour l'API alimentaire au nom de l'utilisateur

#### Gestion du régime alimentaire

- Création et gestion des régimes personnalisés (un seul actif à la fois par utilisateur)
- Intégration avec l'API Open Food Facts pour les données nutritionnelles
- Planification des repas
- Calcul des apports nutritionnels
- Génération de recommandations
- Analyse des tendances nutritionnelles

#### Gestion des repas

- Enregistrement des consommations alimentaires
- Recherche d'aliments via l'API externe (avec mise en cache local)
- Gestion de l'historique des repas
- Calcul des apports nutritionnels par repas
- Vérification optionnelle de la conformité avec le régime actif

---

### 3.3 Gestion de l'authentification

L'authentification sera gérée par Keycloak, un serveur d'identité open-source.

#### Flux d'authentification

1. **Authentification via l'application frontend**
   - L'utilisateur déclenche le processus de connexion depuis l'interface web
   - Il est redirigé vers la page d'authentification centralisée Keycloak
   - S'il possède un compte, il se connecte ; sinon, il crée un compte
   - Une fois authentifié, il est redirigé vers l'application frontend avec un token JWT

2. **Enregistrement côté API métier**
   - À la première connexion, l'application transmet les informations d'identification à l'API
   - L'API crée un profil utilisateur interne lié à l'identifiant Keycloak
   - Ce lien permet d'associer l'identité Keycloak aux données métier de l'utilisateur

3. **Contrôle des accès aux fonctionnalités**
   - Pour chaque requête, un token JWT est transmis dans l'en-tête `Authorization`
   - L'API vérifie la validité du token (signature, expiration, issuer)
   - Les droits d'accès sont vérifiés selon les rôles associés (User, Admin, Nutritionist)

4. **Accès à des services externes**
   - L'API interagit avec Open Food Facts de manière transparente pour l'utilisateur
   - Aucune authentification requise pour Open Food Facts (API publique)

> Pour une description visuelle du flux, voir l'annexe `workflow_Flux_authentification.mermaid`

#### Implémentation de la sécurité

La sécurité est configurée dans `Program.cs` via les middlewares ASP.NET Core :

- `AddAuthentication().AddJwtBearer()` pour la validation des tokens Keycloak
- `AddAuthorization()` avec des policies basées sur les rôles
- Les endpoints sont protégés via les attributs `[Authorize]` et `[Authorize(Roles = "...")]`

---

### 3.4 Paradigmes de développement

#### Domain-Driven Design (DDD)

Le projet est structuré selon les principes du **Domain-Driven Design** — le code s'organise autour du métier, pas autour de la base de données.

| Concept            | Rôle dans ce projet                                                                                |
| ------------------ | -------------------------------------------------------------------------------------------------- |
| **Aggregate Root** | Point d'entrée du domaine — `User`, `Diet`, `DietPlan`, `Meal`, `FoodItem`                         |
| **Entité enfant**  | Vit uniquement via son parent — `MealItem`, `WeightEntry`, `SavedFoodItem`                         |
| **Value Object**   | Groupe de valeurs immuables sans identité — `MacroDistribution`, `NutritionInfo`, `NutritionNeeds` |
| **Domain Service** | Règle métier impliquant plusieurs agrégats — ex: vérification Diet active                          |
| **Invariant**      | Règle toujours vraie, validée dans le constructeur — ex: somme macros = 100%                       |

L'architecture cible est une **architecture 4 couches** (Diamant) où les dépendances convergent vers le Domain. Voir section "Structure du projet" ci-dessous.

---

#### Test-Driven Development (TDD)

Le développement suit le cycle **Red → Green → Refactor**, appliqué couche par couche dans l'ordre d'implémentation.

**Cycle par fonctionnalité :**

1. **Red** — écrire un test qui échoue décrivant le comportement attendu
2. **Green** — écrire le minimum de code pour faire passer le test
3. **Refactor** — nettoyer sans changer le comportement (le test reste vert)

**Ordre d'application par couche :**

| Ordre | Couche         | Ce qu'on teste en TDD                                         | Outils                          |
| ----- | -------------- | ------------------------------------------------------------- | ------------------------------- |
| 1     | Domain         | Invariants, constructeurs, Value Objects, règles métier pures | xUnit + assertions              |
| 2     | Application    | Use cases, orchestration, réponses aux cas d'erreur           | xUnit + Moq (mock repositories) |
| 3     | Infrastructure | Persistance EF Core, requêtes, migrations                     | xUnit + Testcontainers          |
| 4     | API            | Contrats HTTP, codes de retour, format des réponses           | WebApplicationFactory           |

**Convention de nommage des tests :**

```
[Méthode_ou_Comportement]_[Contexte]_[Résultat_attendu]

Exemples :
CreateDiet_WhenNoWeightEntry_Throws422
MacroDistribution_WhenSumIsNot100_ThrowsDomainException
LaunchDiet_WhenDietAlreadyActive_Returns409
```

**Règle structurelle :** chaque projet de test correspond à une couche.

```
NutritionApi.Domain.Tests/          ← tests unitaires Domain
NutritionApi.Application.Tests/     ← tests unitaires Application (Moq)
NutritionApi.Infrastructure.Tests/  ← tests d'intégration (Testcontainers)
NutritionApi.Api.Tests/             ← tests API end-to-end (WebApplicationFactory)
```

---

### 3.5 Technologies utilisées

#### Backend

| Technologie                     | Version | Rôle                                                       |
| ------------------------------- | ------- | ---------------------------------------------------------- |
| **C#**                          | 13      | Langage de programmation principal                         |
| **ASP.NET Core**                | 10      | Framework web pour le développement d'API REST             |
| **Entity Framework Core**       | 10      | ORM pour l'accès aux données et les migrations             |
| **ASP.NET Core Authentication** | 10      | Sécurisation de l'API avec OAuth2 / JWT Bearer             |
| **Keycloak**                    | 24+     | Serveur d'identité et de gestion des accès (OAuth2/OIDC)   |
| **PostgreSQL**                  | 16      | Base de données relationnelle principale (provider Npgsql) |
| **Polly**                       | 8       | Résilience : circuit breaker, retry, timeout               |
| **Hangfire**                    | 1.8+    | Jobs récurrents (import OFF, purge RGPD) + dashboard       |
| **IHttpClientFactory**          | —       | Client HTTP typé pour l'API Open Food Facts                |
| **Swashbuckle**                 | 7       | Documentation OpenAPI / Swagger UI                         |
| **ASP.NET Core Health Checks**  | —       | Endpoints de monitoring (`/health`)                        |
| **OpenTelemetry**               | —       | Instrumentation des métriques et traces distribuées        |
| **Prometheus**                  | —       | Collecte et stockage des métriques                         |
| **Grafana**                     | —       | Visualisation des métriques et tableaux de bord            |

#### Tests

| Technologie                          | Rôle                                               |
| ------------------------------------ | -------------------------------------------------- |
| **xUnit**                            | Framework de tests unitaires                       |
| **Moq**                              | Framework de mock pour les tests unitaires         |
| **Testcontainers for .NET**          | Tests d'intégration avec PostgreSQL en conteneur   |
| **WireMock.Net**                     | Mock de l'API Open Food Facts pour les tests       |
| **Microsoft.AspNetCore.Mvc.Testing** | Tests des contrôleurs avec `WebApplicationFactory` |

#### Métadonnées du projet

| Paramètre               | Valeur                              |
| ----------------------- | ----------------------------------- |
| Commande de création    | `dotnet new webapi -n NutritionApi` |
| Namespace racine        | `NutritionApi`                      |
| Fichier solution        | `NutritionApi.sln`                  |
| Packaging               | Container Docker (Linux)            |
| Runtime cible           | .NET 10                             |
| Gestionnaire de paquets | NuGet                               |

#### Dépendances NuGet principales

| Package                                         | Rôle                             |
| ----------------------------------------------- | -------------------------------- |
| `Microsoft.AspNetCore.Authentication.JwtBearer` | Validation tokens JWT Keycloak   |
| `Microsoft.EntityFrameworkCore`                 | ORM principal                    |
| `Npgsql.EntityFrameworkCore.PostgreSQL`         | Provider PostgreSQL pour EF Core |
| `Swashbuckle.AspNetCore`                        | Documentation OpenAPI            |
| `Polly` + `Microsoft.Extensions.Http.Polly`     | Résilience HTTP                  |
| `Hangfire.AspNetCore` + `Hangfire.PostgreSql`   | Jobs récurrents + stockage état  |
| `OpenTelemetry.Extensions.Hosting`              | Métriques et traces              |
| `prometheus-net.AspNetCore`                     | Exposition métriques Prometheus  |
| `xunit` + `Moq`                                 | Tests unitaires                  |
| `Testcontainers.PostgreSql`                     | Tests d'intégration              |

#### Structure du projet — Architecture DDD 4 couches

Le projet est découpé en **4 projets distincts** dans une solution .NET, conformément à l'architecture DDD.
Règle fondamentale : `Domain` ne dépend de rien. Les dépendances pointent toujours vers le centre.

```
         API
        /   \
  Application  Infrastructure
        \   /
        Domain
```

```
NutritionApi.sln
│
├── NutritionApi.Domain/                   ← 0 dépendance externe
│   ├── Entities/                          ← User, Diet, Meal, MealItem, FoodItem
│   ├── ValueObjects/                      ← MacroDistribution, NutritionNeeds
│   ├── Enums/                             ← ActivityLevel, Goal, DietType, DietStatus, MealType, Gender
│   ├── Interfaces/
│   │   ├── Repositories/                  ← IUserRepository, IDietRepository, IMealRepository...
│   │   └── Services/                      ← IDomainService (règles inter-agrégats)
│   └── Exceptions/                        ← Exceptions métier (DomainException...)
│
├── NutritionApi.Application/              ← dépend uniquement de Domain
│   ├── Services/                          ← NutritionCalculationService, DietService...
│   ├── DTOs/
│   │   ├── Requests/                      ← Corps des requêtes entrantes
│   │   └── Responses/                     ← Corps des réponses sortantes
│   └── Interfaces/                        ← IApplicationService (contrats pour l'API)
│
├── NutritionApi.Infrastructure/           ← dépend de Domain + Application
│   ├── Repositories/                      ← Implémentations EF Core des IRepository
│   ├── Data/
│   │   ├── AppDbContext.cs                ← DbContext EF Core
│   │   └── Migrations/                    ← Migrations auto-générées
│   ├── Jobs/                              ← Jobs Hangfire (import OFF, purge RGPD)
│   └── ExternalServices/
│       └── OpenFoodFacts/                 ← Client HTTP + mise en cache FoodItem
│
└── NutritionApi.API/                      ← point d'entrée, dépend de tout
    ├── Controllers/                        ← Exposition REST (un contrôleur par domaine)
    ├── Middleware/                         ← Gestion globale des erreurs, logging
    └── Configuration/                     ← DI, Auth Keycloak, Swagger, Polly
```

#### Outils et infrastructure

| Outil                    | Rôle                         |
| ------------------------ | ---------------------------- |
| **Docker**               | Conteneurisation de l'API    |
| **Kubernetes**           | Orchestration des conteneurs |
| **GitHub Actions**       | Pipeline CI/CD               |
| **Prometheus + Grafana** | Monitoring et alerting       |
| **dotnet CLI**           | Build, tests, migrations     |

---

### 3.6 Structure des données

#### Modèles principaux

##### User

| Champ              | Type C#            | Description                             |
| ------------------ | ------------------ | --------------------------------------- |
| Id                 | int                | Identifiant unique (auto-incrémenté)    |
| KeycloakId         | string             | UUID lié à l'identité Keycloak (unique) |
| Email              | string             | Adresse email (unique)                  |
| FirstName          | string             | Prénom                                  |
| LastName           | string             | Nom                                     |
| BirthDate          | DateOnly           | Date de naissance                       |
| Gender             | enum Gender        | Sexe (Male / Female / Other)            |
| Height             | float              | Taille en centimètres                   |
| Weight             | float              | Poids en kilogrammes                    |
| ActivityLevel      | enum ActivityLevel | Niveau d'activité physique              |
| Goal               | enum Goal          | Objectif nutritionnel                   |
| Allergies          | List\<string\>     | Liste des allergies alimentaires        |
| DietaryPreferences | List\<string\>     | Préférences alimentaires                |
| CreatedAt          | DateTime           | Date de création du profil              |

##### Diet

| Champ             | Type C#         | Description                                    |
| ----------------- | --------------- | ---------------------------------------------- |
| Id                | int             | Identifiant unique                             |
| UserId            | int             | Référence à l'utilisateur                      |
| Name              | string          | Nom du régime                                  |
| Type              | enum DietType   | Type de régime (Balanced, Keto, etc.)          |
| Status            | enum DietStatus | Statut (Active / Inactive / Archived)          |
| CalorieTarget     | float           | Objectif calorique journalier (kcal)           |
| MacroDistribution | value object    | Répartition protéines / glucides / lipides (%) |
| CreatedAt         | DateTime        | Date de création                               |

> Règle métier : un seul Diet peut avoir le statut `Active` par utilisateur à la fois.

##### Meal

| Champ    | Type C#          | Description                             |
| -------- | ---------------- | --------------------------------------- |
| Id       | int              | Identifiant unique                      |
| UserId   | int              | Référence à l'utilisateur               |
| DietId   | int?             | Référence optionnelle au régime associé |
| Name     | string           | Nom du repas                            |
| Type     | enum MealType    | Type de repas (Breakfast, Lunch, etc.)  |
| DateTime | DateTime         | Date et heure du repas                  |
| Notes    | string?          | Notes libres                            |
| Items    | List\<MealItem\> | Aliments composant le repas             |

##### MealItem

| Champ           | Type C# | Description                      |
| --------------- | ------- | -------------------------------- |
| Id              | int     | Identifiant unique               |
| MealId          | int     | Référence au repas parent        |
| FoodExternalId  | string  | Identifiant dans Open Food Facts |
| FoodName        | string  | Nom de l'aliment                 |
| Quantity        | float   | Quantité consommée en grammes    |
| CaloriesPer100g | float   | Calories pour 100g               |
| ProteinsPer100g | float   | Protéines pour 100g              |
| CarbsPer100g    | float   | Glucides pour 100g               |
| FatsPer100g     | float   | Lipides pour 100g                |

##### FoodItem (cache local Open Food Facts)

| Champ           | Type C#        | Description                          |
| --------------- | -------------- | ------------------------------------ |
| Id              | int            | Identifiant unique local             |
| ExternalId      | string         | Identifiant Open Food Facts (unique) |
| Name            | string         | Nom de l'aliment                     |
| CaloriesPer100g | float          | Calories pour 100g                   |
| ProteinsPer100g | float          | Protéines pour 100g                  |
| CarbsPer100g    | float          | Glucides pour 100g                   |
| FatsPer100g     | float          | Lipides pour 100g                    |
| IsVegetarian    | bool           | Convient aux végétariens             |
| IsVegan         | bool           | Convient aux végétaliens             |
| Allergens       | List\<string\> | Liste des allergènes                 |
| CachedAt        | DateTime       | Date de mise en cache                |

#### Tables de la base de données

Les migrations EF Core génèrent automatiquement le schéma SQL à partir des entités C#.

| Table              | Description                             |
| ------------------ | --------------------------------------- |
| `users`            | Profils utilisateurs                    |
| `user_allergies`   | Allergies (relation 1-N)                |
| `user_preferences` | Préférences alimentaires (relation 1-N) |
| `diets`            | Régimes alimentaires                    |
| `meals`            | Repas enregistrés                       |
| `meal_items`       | Aliments composant chaque repas         |
| `food_items`       | Cache local des données Open Food Facts |

---

### 3.6 API Endpoints

L'API est organisée selon les principes REST :

#### Authentification (via Keycloak)

```
POST   /api/v1/auth/register       // Inscription d'un utilisateur
POST   /api/v1/auth/login          // Connexion et génération de token JWT
POST   /api/v1/auth/refresh        // Rafraîchissement du token
POST   /api/v1/auth/logout         // Déconnexion
```

#### Utilisateurs

```
GET    /api/v1/users/me                  // Récupération du profil utilisateur
PUT    /api/v1/users/me                  // Mise à jour du profil utilisateur
POST   /api/v1/users/me/preferences      // Définition des préférences alimentaires
GET    /api/v1/users/me/stats            // Statistiques de l'utilisateur
```

#### Aliments (via Open Food Facts + cache local)

```
GET    /api/v1/foods/search              // Recherche d'aliments
GET    /api/v1/foods/{externalId}        // Détails d'un aliment
```

#### Régimes alimentaires

```
GET    /api/v1/diets                     // Liste des régimes de l'utilisateur
POST   /api/v1/diets                     // Création d'un régime
GET    /api/v1/diets/{id}                // Détails d'un régime
PUT    /api/v1/diets/{id}                // Mise à jour d'un régime
DELETE /api/v1/diets/{id}                // Suppression d'un régime
PUT    /api/v1/diets/{id}/activate       // Activation d'un régime (désactive les autres)
```

#### Nutrition

```
GET    /api/v1/nutrition/calculate       // Calcul des besoins nutritionnels (BMR, TDEE)
GET    /api/v1/nutrition/daily           // Bilan nutritionnel journalier
GET    /api/v1/nutrition/weekly          // Bilan nutritionnel hebdomadaire
GET    /api/v1/nutrition/recommendations // Recommandations alimentaires
```

#### Repas

```
GET    /api/v1/meals                     // Liste des repas (filtrable par date)
POST   /api/v1/meals                     // Enregistrement d'un repas
GET    /api/v1/meals/{id}                // Détails d'un repas
PUT    /api/v1/meals/{id}                // Mise à jour d'un repas
DELETE /api/v1/meals/{id}                // Suppression d'un repas
```

#### Format de données

Toutes les requêtes et réponses utilisent le format JSON.
Exemple de réponse pour le calcul des besoins nutritionnels :

```json
{
  "bmr": 1762.5,
  "tdee": 2467.5,
  "targetCalories": 1967.5,
  "macroDistribution": {
    "proteins": { "percentage": 25, "grams": 123, "calories": 492 },
    "carbs": { "percentage": 45, "grams": 221, "calories": 884 },
    "fats": { "percentage": 30, "grams": 65, "calories": 585 }
  },
  "goal": "WeightLoss",
  "activityLevel": "ModeratelyActive"
}
```

---

### 3.7 Performances et scalabilité

#### Objectifs de performance

- Temps de réponse moyen < 100ms pour les requêtes simples
- Temps de réponse moyen < 500ms pour les calculs complexes
- Capacité à gérer 200 requêtes/seconde
- Disponibilité > 99.9%

#### Stratégies d'optimisation

- Pagination pour les listes volumineuses
- Mise en cache avec `IMemoryCache` (Caffeine équivalent .NET)
- Exécution asynchrone (`async/await`) pour toutes les opérations I/O
- Circuit breaker Polly pour les appels vers Open Food Facts
- Pool de connexions à la base de données via EF Core / Npgsql

#### Scalabilité

- Architecture sans état (stateless) — scaling horizontal facilité
- Auto-scaling des pods via Kubernetes HPA (CPU/mémoire)
- Rate limiting via middleware ASP.NET Core
- Requêtes EF Core optimisées (pas de `SELECT *`, projections, index)

---

### 3.8 Sécurité

#### Authentification et autorisation

- Authentification via Keycloak avec support OAuth2/OIDC
- Support de fournisseurs externes (Google, Facebook) via Keycloak
- Gestion des rôles : `User`, `Admin`, `Nutritionist`
- Validation des tokens JWT côté serveur à chaque requête
- Vérification des permissions par endpoint via `[Authorize(Roles = "...")]`

#### Protection des données

- Chiffrement des données sensibles au repos
- HTTPS/TLS pour toutes les communications
- Validation des entrées via Data Annotations et FluentValidation
- Protection contre les injections SQL via EF Core (requêtes paramétrées)
- Journalisation des accès et opérations sensibles

#### Sécurité réseau

- Réseau privé Kubernetes pour isoler les composants internes
- Network Policies Kubernetes pour contrôler le trafic inter-pods
- WAF (Web Application Firewall) pour la protection contre les attaques communes

---

### 3.9 Orchestration avec Kubernetes

#### Déploiement et gestion du cycle de vie

| Ressource Kubernetes      | Rôle                                              |
| ------------------------- | ------------------------------------------------- |
| `Deployment`              | Gestion du cycle de vie de l'API (rolling update) |
| `StatefulSet`             | PostgreSQL (données persistantes)                 |
| `ConfigMap`               | Configuration non sensible (URLs, paramètres)     |
| `Secret`                  | Données sensibles (mots de passe, clés API)       |
| `HorizontalPodAutoscaler` | Scaling automatique selon la charge               |
| `Gateway API`             | Routage du trafic externe vers les services       |

#### Environnements

| Environnement | Description                           |
| ------------- | ------------------------------------- |
| `dev`         | Développement local — données de test |
| `staging`     | Recette — miroir de la production     |
| `prod`        | Production — données réelles          |

#### Structure des manifests Kubernetes

```
kubernetes/
├── base/
│   ├── namespace.yaml
│   ├── postgresql.yaml
│   ├── keycloak.yaml
│   ├── nutrition-api.yaml
│   ├── gateway.yaml
│   ├── hpa.yaml
│   ├── configmap.yaml
│   └── secrets.yaml
└── overlays/
    ├── dev/
    ├── staging/
    └── prod/
```

#### Pipeline CI/CD (GitHub Actions)

Le pipeline automatise les étapes suivantes à chaque push :

1. **Build** — `dotnet build`
2. **Tests** — `dotnet test` (unitaires + intégration)
3. **Analyse qualité** — SonarQube
4. **Build image Docker** — `docker build`
5. **Push image** — vers le registry
6. **Déploiement** — `kubectl apply` vers l'environnement cible
